on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
env:
  USER_NM: bzhen
  USER_REPO: ${{ GITHUB.REPOSITORY }}
jobs:
  build:
     runs-on: ubuntu-latest
     steps:
       - name: Checkout
         uses: actions/checkout@v4.1.1
       - name: step 2
         run: |
          ls -l
          echo "github repo name is USER_REPO and branch name is ${{ GITHUB.REF_NAME }} "
          echo "Value for variable user1 is $USER_NM"
          pwd
          java -version
